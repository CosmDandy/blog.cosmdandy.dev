# SHA коммита ветки v5 репозитория jackyzha0/quartz, из которого собрано ядро.
#
# Истории upstream в этом репозитории нет: ядро влито одним коммитом, чтобы в
# логе блога не лежали две тысячи чужих коммитов. Расплата — git не знает общей
# точки и не может слить обновление сам, поэтому обновление делается
# применением диффа между этим SHA и новым. Механика — в `make upgrade`.
#
# Строку меняет только `make upgrade`. Если она разъедется с реальным
# состоянием quartz/, следующий патч ляжет от неверной базы.
507ad7f3d4601d83482f61930fccf1c77f42a072
